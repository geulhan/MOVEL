{
  "title_pattern": "string — title structure formula, e.g. '[인물] + [장소] + [브랜드]'",
  "intro_pattern": "string — opening hook pattern",
  "emotion_words": ["string"],
  "entities": ["string — people, places, organizations"],
  "brands": ["string"],
  "keywords": ["string"],
  "category": "string — primary category",
  "cta": "string — call to action or empty string",
  "writing_style": "string — tone description",
  "paragraph_length": {
    "average_chars": "number",
    "description": "string"
  },
  "seo_keywords": ["string"],
  "confidence": "number 0-1",
  "new_patterns": {
    "new_words": ["string"],
    "new_expressions": ["string"]
  }
}
